import React from "react";
import { createRoot } from "react-dom/client";
import NakheelSystemRoot from "./NakheelSystem.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NakheelSystemRoot />
  </React.StrictMode>
);
